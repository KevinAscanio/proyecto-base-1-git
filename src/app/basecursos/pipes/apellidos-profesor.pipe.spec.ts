import { ApellidosProfesorPipe } from './apellidos-profesor.pipe';

describe('ApellidosProfesorPipe', () => {
  it('create an instance', () => {
    const pipe = new ApellidosProfesorPipe();
    expect(pipe).toBeTruthy();
  });
});
