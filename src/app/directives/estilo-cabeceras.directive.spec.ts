import { EstiloCabecerasDirective } from './estilo-cabeceras.directive';

describe('EstiloCabecerasDirective', () => {
  it('should create an instance', () => {
    const directive = new EstiloCabecerasDirective();
    expect(directive).toBeTruthy();
  });
});
